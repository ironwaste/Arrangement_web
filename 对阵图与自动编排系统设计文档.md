# 对阵图生成与自动编排系统技术文档

## 一、系统概述

本系统用于跆拳道比赛的对阵编排管理，核心功能包括：
- 自动签位分配（Draw Number Assignment）
- 对阵图生成（Bracket Generation）
- 对阵表编排（Match Scheduling）
- 场地单元分配（Venue & Unit Allocation）

---

## 二、核心数据结构

### 2.1 运动员表（athletes）
```javascript
{
  id: 运动员ID,
  athlete_name: "姓名",
  athlete_team: "代表队",
  athlete_category: "级别（公斤级）",
  athlete_gender: "男/女",
  athlete_age_group: "年龄组（小学/初中/高中/成年）",
  athlete_draw_num: 签位数（1-N）,  // 0表示未分配
  event_id: 赛事ID
}
```

### 2.2 级别模式表（ModeOfCompetition）
```javascript
{
  class_: "级别名称",
  mode_of_competition: "竞赛方式"  // 循环赛/5人循环赛-2/6人循环赛/7人循环赛/round_robin/single_elimination
}
```

### 2.3 编排方案表（category_mode）
```javascript
{
  event_id: 赛事ID,
  weight_class: 级别名称,
  category_venue: "场地代码(A-Z)",
  category_date_num: 单元序号(1-N),
  category_order: 该级别的比赛顺序
}
```

### 2.4 对阵阶段表（bracket_stage_map）
```javascript
{
  event_id: 赛事ID,
  category_id: 级别,
  stage_id: 阶段ID（可多个，用逗号分隔）,
  stage_type: "single_elimination|round_robin|divisional_round_robin"
}
```

---

## 三、签位分配逻辑

### 3.1 标准签位顺序生成算法

**文件位置**: [routes/autoScheduler.js:1-16](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/autoScheduler.js#L1-L16)

**算法原理**: 采用"镜像对称"算法生成锦标赛签位表，确保种子选手在后半区相遇。

```javascript
function generateStandardSeedOrder(size) {
  // 2人: [1, 2]
  // 4人: [1, 4, 2, 3]
  // 8人: [1, 8, 5, 4, 2, 7, 6, 3]
  // 16人: [1, 16, 9, 8, 5, 12, 13, 4, 2, 15, 10, 7, 6, 11, 14, 3]
}
```

**数学原理**:
1. 从最小对阵数开始（如2人）
2. 每次翻倍时，将已有序列按特定规则展开
3. 新序列遵循"首尾镜像"原则，确保强手不会过早相遇

### 3.2 签位分配策略

**文件位置**: [routes/events.js:1629-1695](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/events.js#L1629-L1695)

#### 分配流程:

1. **数据准备**
   ```javascript
   // 1. 清零所有运动员签位
   await db.run('UPDATE athletes SET athlete_draw_num = 0 WHERE id = ?', [a.id]);
   
   // 2. 计算目标签位数（向上取整到2的幂）
   const n = classAthletes.length;
   const targetSize = Math.pow(2, Math.ceil(Math.log2(n)));  // 如7人 -> 8
   const seedOrderFull = generateStandardSeedOrder(targetSize);
   ```

2. **运动员分类**
   ```javascript
   // 已种子选手（已有签位且有效）
   const seeded = classAthletes.filter(a => 
     a.athlete_draw_num > 0 && a.athlete_draw_num <= n
   ).sort((a, b) => a.athlete_draw_num - b.athlete_draw_num);
   
   // 非种子选手（无签位或签位无效）
   const unseeded = classAthletes.filter(a => 
     a.athlete_draw_num === 0 || a.athlete_draw_num > n
   );
   ```

3. **同队回避算法**
   ```javascript
   // 核心思想：同队运动员应尽量晚相遇（最大距离原则）
   
   for (const athlete of unseeded) {
     const athlete_team = athlete.athlete_team;
     const unitPositions = unitPositionMap.get(athlete_team) || [];
     
     let bestPos = null;
     let bestScore = -1;
     
     for (let i = 0; i < remainingPositions.length; i++) {
       const pos = remainingPositions[i];
       const bracketPos = seedToBracketPos[pos];
       
       // 计算与同队选手的最小距离
       let minDistance = targetSize;
       for (const up of unitPositions) {
         const upBracketPos = seedToBracketPos[up];
         const dist = Math.abs(bracketPos - upBracketPos);
         if (dist < minDistance) minDistance = dist;
       }
       
       // 评分规则：距离越远分数越高，同等距离优先后半区
       const score = minDistance * 100 + (targetSize - bracketPos);
       if (score > bestScore) {
         bestScore = score;
         bestPos = i;
       }
     }
     
     // 分配最优位置
     const assignedPos = remainingPositions.splice(bestPos >= 0 ? bestPos : 0, 1)[0];
     unseededAssignments.push({ athlete, position: assignedPos });
     unitPositionMap.get(athlete_team).push(assignedPos);
   }
   ```

**同队回避效果示例**:
- 假设有A队的3名运动员和B队的3名运动员
- 签位分配应尽量使A队运动员分散在不同半区
- 避免同队选手在第一轮就相遇

---

## 四、对阵图生成逻辑

### 4.1 竞赛模式判断

**文件位置**: [routes/autoScheduler.js:131-143](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/autoScheduler.js#L131-L143)

```javascript
// 支持的循环赛模式
const roundRobinMethods = ['循环赛', '5人循环赛-1', '5人循环赛-2', '6人循环赛', '7人循环赛', 'round_robin'];

// 判断是否为分站式循环赛
const isDivisional = ['5人循环赛-2', '6人循环赛', '7人循环赛'].includes(method);

// 判断是否为普通循环赛
const isRoundRobin = method && roundRobinMethods.includes(method);
```

### 4.2 循环赛对阵生成

**文件位置**: [routes/autoScheduler.js:18-47](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/autoScheduler.js#L18-L47)

**算法**: 轮转编排法
```javascript
function generateRoundRobinMatches(n, method) {
  // 1. 初始化位置数组
  const positions = [1, 2, 3, ..., n];
  
  // 2. 轮转：固定一位置，其余位置顺时针旋转
  // 第1轮: [1, 2, 3, ..., n]
  // 第2轮: [1, n, 2, 3, ..., n-1]
  // 第3轮: [1, n-1, n, 2, 3, ..., n-2]
  // ...
  
  // 3. 每轮对阵：position[i] vs position[n-1-i]
}
```

**示例**（6人循环赛）:
```
第1轮: 1-6, 2-5, 3-4
第2轮: 1-5, 6-4, 2-3
第3轮: 1-4, 5-3, 6-2
第4轮: 1-3, 4-2, 5-6
第5轮: 1-2, 3-6, 4-5
```

### 4.3 分站循环赛（上区+下区+决赛）

**文件位置**: [routes/autoScheduler.js:180-261](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/autoScheduler.js#L180-L261)

适用场景: 5-7人级别

| 模式 | 上区人数 | 下区人数 | 决赛 |
|------|----------|----------|------|
| 5人循环赛-2 | 3人 | 2人 | 有 |
| 6人循环赛 | 3人 | 3人 | 有 |
| 7人循环赛 | 3人 | 4人 | 有 |

**流程**:
1. 将运动员按签位分配到上区/下区
2. 上区进行循环赛
3. 下区进行循环赛
4. 上下区胜者进行决赛

### 4.4 单败淘汰赛对阵图

**文件位置**: [routes/autoScheduler.js:263-326](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/autoScheduler.js#L263-L326)

**生成流程**:
```javascript
// 1. 计算目标签位数
const targetSize = Math.pow(2, Math.ceil(Math.log2(n)));

// 2. 补齐空位（Bye轮空）
while (cleanSeeding.length < targetSize) {
  cleanSeeding.push(null);  // null表示轮空
}

// 3. 按照标准签位顺序创建对阵阶段
const stage = await manager.create.stage({
  tournamentId: 1,
  name: weightClass,
  type: 'single_elimination',
  seeding: cleanSeeding,
  settings: {
    manualOrdering: [seedOrderFull],  // 使用标准签位顺序
  },
});

// 4. 更新轮次名称（1/决赛, 1/2, 1/4, 1/8, ...）
```

---

## 五、比赛场次计算

### 5.1 场次计算公式

**文件位置**: [public/js/brackets.js:1765-1798](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/js/brackets.js#L1765-L1798)

```javascript
function calculateRounds(count) {
  // 单败淘汰赛总场次 = n - 1（n为参赛人数）
  // 例: 8人 -> 7场 (决赛1场 + 半决赛2场 + 四分之一决赛4场)
  
  // 奖牌计算
  // 金牌: 1枚
  // 银牌: 1枚
  // 铜牌: 2枚（半决赛负者）
  
  // 轮次分配
  // 16人: 首轮(8场) + 1/4决赛(4场) + 半决赛(2场) + 决赛(1场) = 15场
  // 8人:  首轮(4场) + 半决赛(2场) + 决赛(1场) = 7场
  // 7人:  首轮(3场) + 1/4决赛(4场) + 半决赛(2场) + 决赛(1场) = 10场
}
```

---

## 六、完整数据流

### 6.1 自动编排主流程

**前端入口**: [public/js/brackets.js:1800-1823](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/js/brackets.js#L1800-L1823)

```
用户点击"执行自动编排"
    ↓
前端调用 POST /auto-arrange
    ↓
后端自动编排逻辑启动 [routes/events.js:1568-1725]
    ↓
┌──────────────────────────────────────┐
│  遍历所有级别（按athlete_category分组）│
└──────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────┐
│  对每个级别执行签位分配算法               │
│  1. 读取该级别所有运动员                  │
│  2. 区分种子选手和非种子选手              │
│  3. 种子选手保持原签位                    │
│  4. 非种子选手按同队回避原则分配          │
│  5. 更新数据库athlete_draw_num字段       │
└──────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────┐
│  调用对阵图生成函数                        │
│  generateBracketForClass()               │
│  [routes/autoScheduler.js:69-340]         │
│                                          │
│  1. 删除旧的对阵数据                      │
│  2. 查询该级别的竞赛模式                  │
│  3. 根据模式选择生成算法                  │
│  4. 创建bracket_stage记录                │
│  5. 创建bracket_round记录                │
│  6. 创建bracket_match记录                │
│  7. 创建bracket_participant记录          │
│  8. 更新bracket_stage_map                │
└──────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────┐
│  调用比赛重排函数                         │
│  reorderMatches()                        │
└──────────────────────────────────────────┘
    ↓
返回结果给前端
    ↓
前端刷新对阵编排界面
```

### 6.2 对阵图渲染流程

**前端入口**: [public/js/bracket-detail.js:471-487](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/js/bracket-detail.js#L471-L487)

```
用户双击某个级别
    ↓
前端调用 GET /brackets/stage-id/{weightClass}
    ↓
获取该级别的stage_id
    ↓
前端调用 GET /brackets/stage/{stageId}
    ↓
获取完整的对阵数据结构
    ↓
┌──────────────────────────────────────────┐
│  使用brackets-viewer库渲染对阵图           │
│  [public/lib/brackets-viewer.min.js]      │
│                                          │
│  配置项:                                  │
│  - selector: '#bracket-viewer-container'  │
│  - showRankingTable: false                │
│  - participantOriginPlacement: 'none'    │
│  - customRoundName: 自定义轮次名称        │
└──────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────┐
│  后处理：添加附加信息                      │
│  1. 场地编号标注                          │
│  2. 签位号显示                            │
│  3. 代表队标签                            │
│  4. 处理轮空显示                          │
└──────────────────────────────────────────┘
    ↓
显示对阵图
```

---

## 七、关键API接口

### 7.1 自动编排接口
```
POST /auto-arrange
参数:
  - event_id: 赛事ID（必填）
  - weight_class: 级别名称（可选，不填则编排所有级别）
  - category_venue: 场地代码
  - category_date_num: 单元序号
  - category_order: 比赛顺序

响应:
{
  success: true,
  data: {
    generated: 10,      // 成功生成的级别数
    skipped: 2,         // 跳过的级别数
    errors: ["级别: 运动员不足2人，跳过"],
    results: ["女子46kg: 7人，对阵已生成"]
  }
}
```

### 7.2 编排方案保存接口
```
POST /auto-arrange/save
POST /auto-arrange/scheme
GET  /auto-arrange/scheme?event_id=xxx
```

### 7.3 对阵图接口
```
GET  /brackets/stage-map?event_id=xxx
GET  /brackets/stage-id/{weightClass}?event_id=xxx
GET  /brackets/stage/{stageId}
POST /brackets/rebuild-match-ids
```

---

## 八、数据库表结构

### 8.1 bracket_stage（对阵阶段）
```sql
CREATE TABLE bracket_stage (
  id INT PRIMARY KEY,
  tournament_id INT,
  name VARCHAR(255),
  type VARCHAR(50),        -- single_elimination, round_robin, divisional_round_robin
  settings JSON,
  created_at TIMESTAMP
);
```

### 8.2 bracket_round（对阵轮次）
```sql
CREATE TABLE bracket_round (
  id INT PRIMARY KEY,
  stage_id INT,
  number INT,              -- 轮次编号（1=首轮）
  name VARCHAR(50),       -- 轮次名称（1/16, 1/8, 1/4, 1/2, 决赛）
  bracket_draw INT
);
```

### 8.3 bracket_match（对阵比赛）
```sql
CREATE TABLE bracket_match (
  id INT PRIMARY KEY,
  stage_id INT,
  round_id INT,
  bracket_draw INT,
  bracket_position INT,
  opponent1 JSON,         -- {"id": 1, "name": "张三", "seed": 1}
  opponent2 JSON,
  winner_id INT,
  child_count INT,
  next_match_id INT
);
```

### 8.4 bracket_match_game（附加比赛场次）
```sql
CREATE TABLE bracket_match_game (
  id INT PRIMARY KEY,
  stage_id INT,
  parent_id INT,          -- 关联bracket_match.id
  number INT,             -- 场次编号
  game_count INT,         -- 该场比赛的第几局
  winner VARCHAR(50)
);
```

### 8.5 bracket_participant（参赛者/签位）
```sql
CREATE TABLE bracket_participant (
  id INT PRIMARY KEY,
  tournament_id INT,
  name VARCHAR(255),
  custom_data JSON,       -- {"id": 运动员ID, "athlete_draw_num": 签位号}
  origin INT
);
```

---

## 九、核心算法总结

### 9.1 签位分配算法特点
- **标准签位顺序**: 使用镜像对称算法，确保种子选手在决赛前不会相遇
- **同队回避**: 通过计算同队选手间的最小距离，尽量将同队选手分散
- **随机性**: 非种子选手使用Fisher-Yates洗牌算法引入随机性
- **种子保护**: 已分配签位的种子选手保持原有位置

### 9.2 循环赛编排算法特点
- **轮转编排**: 固定一个位置，其余位置顺时针轮转
- **公平性**: 确保每队运动员比赛场次相同
- **分站循环**: 将运动员分为上区/下区，减少比赛场次

### 9.3 对阵图生成算法特点
- **多模式支持**: 支持单败淘汰、循环赛、分站循环赛等多种赛制
- **数据完整性**: 自动处理轮空（Bye）情况
- **结构化存储**: 使用bracket_manager库管理对阵数据，支持复杂查询

---

## 十、文件索引

| 功能模块 | 文件路径 |
|---------|---------|
| 前端对阵编排界面 | [public/js/brackets.js](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/js/brackets.js) |
| 前端对阵图展示 | [public/js/bracket-detail.js](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/js/bracket-detail.js) |
| 前端对阵图测试 | [public/js/bracket-test.js](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/js/bracket-test.js) |
| 后端编排API | [routes/events.js](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/events.js) |
| 对阵图生成核心算法 | [routes/autoScheduler.js](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/routes/autoScheduler.js) |
| 对阵图渲染库 | [public/lib/brackets-viewer.min.js](file:///d:/bieren/V1/mysql-version/TaekwondoManager_v1_test/public/lib/brackets-viewer.min.js) |

---

*文档生成时间: 2026-05-17*
