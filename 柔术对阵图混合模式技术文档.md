# 柔术对阵图生成 - 混合模式技术文档

## 概述

本系统采用**混合模式**生成和管理柔术对阵图：
- **bracket-manager API**：用于创建对阵图的基本结构和算法逻辑
- **直接 SQL 操作**：用于补充业务数据和自定义字段

这种混合方式既利用了 bracket-manager 的强大算法能力，又满足了业务系统的自定义需求。

---

## 一、bracket-manager API 部分

### 1.1 单败淘汰赛 (single_elimination)

**代码位置**：`routes/Jiu-Jitsu/jiu-jitsu-bracket-helpers.js` 第 571-585 行

```javascript
// 使用 bracket-manager 创建单败淘汰赛 stage
const stage = await manager.create.stage({
    tournamentId: Number(event_id),           // 赛事ID（bracket-manager 要求的参数）
    name: weightClass,                        // 级别名称，如 "70kg"
    type: 'single_elimination',               // 赛制类型：单败淘汰赛
    seeding: cleanSeeding,                    // 种子列表（运动员姓名数组）
    settings: {
        manualOrdering: [seedOrder],          // 手动指定种子排序（使用自定义算法）
    },
});
```

**说明**：
- `manager.create.stage()` 是 bracket-manager 的核心 API
- 自动生成对阵图的结构：轮次、比赛场次、种子位置等
- `seedOrder` 使用自定义的 `generateJJSeedOrder()` 函数生成，确保种子排序符合柔术比赛规则

---

### 1.2 双败淘汰赛 (double_elimination)

**代码位置**：`routes/Jiu-Jitsu/jiu-jitsu-bracket-helpers.js` 第 319-333 行

```javascript
// 使用 bracket-manager 创建双败淘汰赛 stage
const deStage = await manager.create.stage({
    tournamentId: Number(event_id),
    name: weightClass,
    type: 'double_elimination',               // 赛制类型：双败淘汰赛
    seeding: cleanSeeding,
    settings: {
        manualOrdering: [seedOrder],          // 手动指定种子排序
    },
});
```

**说明**：
- 自动生成胜者组和败者组的完整结构
- 包含 Grand Final（总决赛）的逻辑
- bracket-manager 会自动计算败者组的晋级路径

---

### 1.3 单循环赛 (round_robin)

**代码位置**：`routes/Jiu-Jitsu/jiu-jitsu-bracket-helpers.js` 第 430-439 行

```javascript
// 使用 bracket-manager 创建单循环赛 stage
const rrStage = await manager.create.stage({
    tournamentId: Number(event_id),
    name: weightClass,
    type: 'round_robin',                      // 赛制类型：单循环赛
    seeding: effectiveSeeding,                // 有效种子列表（过滤掉 null）
    settings: { 
        size: n,                              // 参赛人数
        groupCount: 1                         // 分组数量：1个组（所有人在一起循环）
    },
});
```

**说明**：
- 自动生成循环赛的所有比赛场次
- 每个运动员都会与其他所有运动员比赛一次
- bracket-manager 使用 Berger 表算法生成赛程

---

### 1.4 分区循环赛 (pool_elimination)

**代码位置**：`routes/Jiu-Jitsu/jiu-jitsu-bracket-helpers.js` 第 491-500 行

```javascript
// 使用 bracket-manager 创建分区循环赛 stage（上区和下区）
const divisionalStage = await manager.create.stage({
    tournamentId: Number(event_id),
    name: `${weightClass}_分区循环赛`,
    type: 'round_robin',                      // 类型仍然是循环赛
    seeding: combinedSeeding,                 // 合并的种子列表：上区 + 下区
    settings: {
        groupCount: 2,                        // 分组数量：2个组（上区和下区）
        manualOrdering: [upperNum, lowerNum], // 手动指定分组：上区种子编号 + 下区种子编号
    },
});

// 创建决赛 stage（上区第一 vs 下区第一）
const finalStage = await manager.create.stage({
    tournamentId: Number(event_id),
    name: `${weightClass}_决赛`,
    type: 'round_robin',                      // 决赛也使用循环赛类型（实际只有1场比赛）
    settings: { groupCount: 1 },
    seeding: ['上区第一', '下区第一'],         // 占位符种子
});
```

**说明**：
- 分区循环赛分为两个阶段：分区循环 + 决赛
- 上区和下区分别进行循环赛，各组第一名进入决赛
- `manualOrdering` 手动指定哪些种子进入上区，哪些进入下区

---

## 二、直接 SQL 操作部分

### 2.1 更新 bracket_stage 表（补充业务字段）

**代码位置**：所有赛制通用，例如第 587-590 行

```javascript
// bracket-manager 创建 stage 后，需要补充 event_id 和 category_id
await db.run(
    'UPDATE bracket_stage SET event_id = ?, category_id = ?, mode_category_id = ? WHERE id = ?',
    [
        event_id,                             // 赛事ID（业务系统需要）
        weightClass,                          // 级别名称（业务系统需要）
        categoryId ? Number(categoryId) : null, // 模式类别ID（关联到 category_mode 表）
        stage.id                              // bracket-manager 生成的 stage ID
    ]
);
```

**说明**：
- bracket-manager 只设置了 `tournament_id`，但业务系统需要 `event_id` 和 `category_id`
- `mode_category_id` 用于关联到 `category_mode` 表，获取比赛模式信息
- 这些字段是业务系统查询和筛选对阵图的关键字段

---

### 2.2 更新 bracket_participant 表（补充自定义数据）

**代码位置**：所有赛制通用，例如第 622-634 行

```javascript
// 为每个 participant 补充 custom_data（包含运动员ID、签号、单位等信息）
for (let i = 0; i < sortedAthletes.length; i++) {
    const athlete = sortedAthletes[i] || {};
    const bracketName = cleanSeeding[i];
    if (!bracketName) continue;
    
    await db.run(
        'UPDATE bracket_participant SET custom_data = ? WHERE name = ?',
        [
            JSON.stringify({
                id: athlete.id != null ? athlete.id : null,           // 运动员ID（关联到 athletes 表）
                athlete_draw_num: athlete.athlete_draw_num != null 
                    ? athlete.athlete_draw_num : (i + 1),             // 签号（比赛顺序号）
                athlete_team: athlete.athlete_team || ''              // 单位/队伍
            }),
            bracketName                                            // participant 的名称（用于匹配）
        ]
    );
}
```

**说明**：
- bracket-manager 只保存了 participant 的 `name`，但业务系统需要更多信息
- `custom_data` 是一个 JSON 字符串，包含：
  - `id`：运动员在业务系统中的唯一标识
  - `athlete_draw_num`：签号（决定比赛顺序和种子位置）
  - `athlete_team`：单位/队伍名称
- 这些信息用于渲染对阵图时显示签号，以及同步到 `jiu_jitsu_matchs` 表

---

### 2.3 手动创建 bracket_match_game 表（比赛局数）

**代码位置**：所有淘汰赛赛制通用，例如第 592-599 行

```javascript
// bracket-manager 不自动创建 match_game 记录，需要手动添加
const matches = await db.all('SELECT id FROM bracket_match WHERE stage_id = ?', [stage.id]);
for (const match of matches) {
    const existing = await db.get('SELECT id FROM bracket_match_game WHERE parent_id = ?', [match.id]);
    if (!existing) {
        // 为每场比赛创建一个 match_game 记录（默认1局）
        await db.run(
            'INSERT INTO bracket_match_game (stage_id, parent_id, number) VALUES (?, ?, ?)',
            [stage.id, match.id, 1]
        );
        // 更新 match 的 child_count（表示这场比赛有1局）
        await db.run('UPDATE bracket_match SET child_count = 1 WHERE id = ?', [match.id]);
    }
}
```

**说明**：
- `bracket_match_game` 表存储每场比赛的局数信息
- bracket-manager 不自动创建这个表，需要手动补充
- `child_count` 表示这场比赛包含多少局
- 柔术比赛默认每场1局，但可以通过这个表支持多局比赛

---

### 2.4 更新 bracket_round 表（自定义轮次名称）

**代码位置**：淘汰赛赛制，例如第 601-620 行

```javascript
// 自定义轮次名称：Final, 1/2, 1/4, 1/8 等
const stageData = await manager.get.stageData(stage.id);
if (stageData?.stage?.[0]) {
    const s = stageData.stage[0];
    const size = s.settings?.size || stageData.participant?.length || 4;
    const totalRounds = Math.log2(Math.pow(2, Math.ceil(Math.log2(size))));
    const rounds = stageData.round || [];
    
    for (const round of rounds) {
        let roundName;
        if (round.number && totalRounds) {
            if (round.number === totalRounds) {
                roundName = 'Final';                    // 决赛
            } else {
                const d = Math.pow(2, totalRounds - round.number);
                roundName = `1/${d}`;                   // 半决赛、四分之一决赛等
            }
        }
        if (roundName && round.name !== roundName) {
            await db.run('UPDATE bracket_round SET name = ? WHERE id = ?', [roundName, round.id]);
        }
    }
}
```

**说明**：
- bracket-manager 默认的轮次名称是 "Round 1", "Round 2" 等
- 业务系统需要更直观的名称："Final", "1/2", "1/4", "1/8" 等
- 通过计算总轮数和当前轮次，动态生成合适的轮次名称

---

### 2.5 手动创建循环赛比赛场次

**代码位置**：单循环赛专用，第 96-146 行

```javascript
// bracket-manager 的 round_robin 不生成 match 记录，需要手动创建
async function createJJRoundRobinBracketMatches(db, stageId, effectiveSeeding, participantList) {
    const n = effectiveSeeding.length;
    if (n < 2) return;

    // 清空现有的 match、round、match_game 记录
    await db.run('DELETE FROM bracket_match_game WHERE stage_id = ?', [stageId]);
    await db.run('DELETE FROM bracket_match WHERE stage_id = ?', [stageId]);
    await db.run('DELETE FROM bracket_round WHERE stage_id = ?', [stageId]);

    // 获取 group ID
    const groupRow = await db.get('SELECT id FROM bracket_group WHERE stage_id = ?', [stageId]);
    const groupId = groupRow ? groupRow.id : null;

    // 建立 seed 编号到 participant ID 的映射
    const seedingToParticipant = new Map();
    for (let i = 0; i < effectiveSeeding.length; i++) {
        const p = participantList.find(pp => pp.name === effectiveSeeding[i]);
        if (p) {
            seedingToParticipant.set(i + 1, p.id);    // seed 1 -> participant ID 1
        }
    }

    // 使用 Berger 表算法生成循环赛赛程
    const rrMatches = generateJJRoundRobinSchedule(n);   // 返回每一轮的比赛配对

    let matchNumber = 1;
    for (let roundIdx = 0; roundIdx < rrMatches.length; roundIdx++) {
        // 确定轮次名称
        let roundName;
        if (n === 2) {
            roundName = 'Final';
        } else {
            roundName = `R${roundIdx + 1}`;             // R1, R2, R3...
        }

        // 创建轮次记录
        const roundResult = await db.run(
            'INSERT INTO bracket_round (stage_id, group_id, name, number) VALUES (?, ?, ?, ?)',
            [stageId, groupId, roundName, roundIdx + 1]
        );
        const newRound = await db.get('SELECT LAST_INSERT_ID() as id');
        const roundId = newRound.id;

        // 为该轮次的每场比赛创建 match 记录
        for (const match of rrMatches[roundIdx]) {
            const opp1ParticipantId = seedingToParticipant.get(match.seed1);
            const opp2ParticipantId = seedingToParticipant.get(match.seed2);

            // opponent1 和 opponent2 是 JSON 格式，包含 participant ID
            const opponent1 = opp1ParticipantId ? JSON.stringify({ id: opp1ParticipantId }) : null;
            const opponent2 = opp2ParticipantId ? JSON.stringify({ id: opp2ParticipantId }) : null;

            await db.run(
                'INSERT INTO bracket_match (stage_id, round_id, group_id, number, child_count, opponent1, opponent2, status) VALUES (?, ?, ?, ?, ?, ?, ?, ?)',
                [stageId, roundId, groupId, matchNumber, 0, opponent1, opponent2, 2]   // status=2 表示未开始
            );
            matchNumber++;
        }
    }
}
```

**说明**：
- bracket-manager 的 `round_robin` 类型不自动生成 `bracket_match` 记录
- 需要使用自定义的 Berger 表算法生成循环赛赛程
- `generateJJRoundRobinSchedule(n)` 返回一个二维数组，每个元素是一轮的比赛配对
- 手动创建 `bracket_round` 和 `bracket_match` 记录

---

## 三、同步到业务表（jiu_jitsu_matchs）

### 3.1 从 bracket 表读取数据

**代码位置**：`routes/Jiu-Jitsu/jiu-jitsu-bracket-helpers.js` 第 883-997 行

```javascript
// 从 bracket_stage 表读取所有 stage
const stageMapRows = await db.all(
    'SELECT id AS stage_id, type AS stage_type, name AS stage_name FROM bracket_stage WHERE event_id = ? AND category_id = ?',
    [event_id, weightClass]
);

// 从 bracket_participant 表读取所有 participant 及其 custom_data
const participants = await db.all(
    'SELECT id, name, custom_data FROM bracket_participant WHERE tournament_id = ?',
    [Number(event_id)]
);

// 解析 custom_data，提取运动员ID、签号、单位等信息
const participantMap = new Map();
participants.forEach(p => {
    let info = { name: p.name, id: null, athlete_draw_num: null, unit: '', zone: '' };
    try {
        if (p.custom_data) {
            const custom = JSON.parse(p.custom_data);
            info.id = custom.id;                          // 运动员ID
            info.athlete_draw_num = custom.athlete_draw_num;  // 签号
            info.unit = custom.athlete_team || '';        // 单位
            info.zone = custom.zone || '';                // 分区（上区/下区）
        }
    } catch (e) {}
    participantMap.set(p.id, info);
});

// 从 bracket_match 表读取所有比赛场次
const bracketMatches = await db.all(
    `SELECT bm.id, bm.number, bm.opponent1, bm.opponent2, bm.status, bm.group_id,
            br.number AS round_number, br.name AS round_name
     FROM bracket_match bm
     LEFT JOIN bracket_round br ON bm.round_id = br.id
     WHERE bm.stage_id = ?
     ORDER BY br.number, bm.number`,
    [numSid]
);
```

**说明**：
- 从 bracket 相关表读取对阵图数据
- 解析 `custom_data` 获取签号等业务信息
- 准备同步到 `jiu_jitsu_matchs` 表

---

### 3.2 写入到 jiu_jitsu_matchs 表

**代码位置**：`routes/Jiu-Jitsu/jiu-jitsu-bracket-helpers.js` 第 1128-1281 行

```javascript
// 遍历每场比赛，提取红方和蓝方运动员信息
for (const bm of allBracketMatches) {
    let blueName = '', redName = '';
    let blueAthleteId = null, redAthleteId = null;
    let blueUnit = '', redUnit = '';
    let blueDrawNum = null, redDrawNum = null;    // 签号信息
    let zone = '';

    // 解析 opponent1（红方）
    try {
        if (bm.opponent1) {
            const opp1 = typeof bm.opponent1 === 'string' ? JSON.parse(bm.opponent1) : bm.opponent1;
            if (opp1 && opp1.id) {
                const info = participantMap.get(opp1.id);
                if (info) {
                    redName = info.name || '';
                    redAthleteId = info.id;
                    redUnit = info.unit || '';
                    redDrawNum = info.athlete_draw_num;    // 提取红方签号
                    zone = info.zone || '';
                }
            }
        }
    } catch (e) {}

    // 解析 opponent2（蓝方）
    try {
        if (bm.opponent2) {
            const opp2 = typeof bm.opponent2 === 'string' ? JSON.parse(bm.opponent2) : bm.opponent2;
            if (opp2 && opp2.id) {
                const info = participantMap.get(opp2.id);
                if (info) {
                    blueName = info.name || '';
                    blueAthleteId = info.id;
                    blueUnit = info.unit || '';
                    blueDrawNum = info.athlete_draw_num;    // 提取蓝方签号
                    if (!zone) zone = info.zone || '';
                }
            }
        }
    } catch (e) {}

    // 确定比赛状态
    let matchStatus;
    if (bracketStatus === 4) {
        matchStatus = '已结束';
    } else if (bracketStatus === 3) {
        matchStatus = '进行中';
    } else if (!redName || !blueName) {
        matchStatus = 'bye';                       // 轮空
    } else {
        matchStatus = '未开始';
    }

    // 确定轮次编号（决赛用 999）
    let roundNum = bm.round_number || 1;
    const rn = bm.round_name || '';
    if (rn === 'Final' || rn === 'D.Final' || rn === 'R.Final') {
        roundNum = 999;
    }

    // 写入到 jiu_jitsu_matchs 表
    await db.run(
        `INSERT INTO jiu_jitsu_matchs
        (event_id, jiu_jitsu_match_venue, jiu_jitsu_match_id, jiu_jitsu_match_categroy,
         jiu_jitsu_match_round_num, jiu_jitsu_match_round_name, jiu_jitsu_match_category_total_rounds,
         jiu_jitsu_bracket_match_id,
         jiu_jitsu_red_athlete_id, jiu_jitsu_red_athlete_name, jiu_jitsu_red_athlete_team, jiu_jitsu_red_athlete_draw_num,
         jiu_jitsu_blue_athlete_id, jiu_jitsu_blue_athlete_name, jiu_jitsu_blue_athlete_team, jiu_jitsu_blue_athlete_draw_num,
         jiu_jitsu_match_comp_mode, jiu_jitsu_match_zone,
         jiu_jitsu_blue_prev_bracket_match_id, jiu_jitsu_red_prev_bracket_match_id,
         jiu_jitsu_match_status)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
        [
            event_id, preservedVenue, preservedMatchId, weightClass,
            roundNum, rn, maxRoundNumber,
            bm.id,
            redAthleteId, redName, redUnit, redDrawNum,        // 红方信息（包含签号）
            blueAthleteId, blueName, blueUnit, blueDrawNum,    // 蓝方信息（包含签号）
            effectiveCompMode || null, zone || null,
            bluePrevBracketMatchId, redPrevBracketMatchId,
            matchStatus
        ]
    );
}
```

**说明**：
- 从 `bracket_match` 的 `opponent1` 和 `opponent2` 提取运动员信息
- 从 `participantMap` 获取签号、单位等业务数据
- 写入到 `jiu_jitsu_matchs` 表，包含签号字段：
  - `jiu_jitsu_red_athlete_draw_num`：红方签号
  - `jiu_jitsu_blue_athlete_draw_num`：蓝方签号
- 这些签号信息用于前端渲染对阵图时显示

---

## 四、前端渲染（读取签号并显示）

### 4.1 从 jiu_jitsu_matchs 表读取数据

**代码位置**：前端渲染器会从 API 获取 `jiu_jitsu_matchs` 数据

```javascript
// API 返回的数据结构（包含签号字段）
{
    jiu_jitsu_red_athlete_name: "张三",
    jiu_jitsu_red_athlete_team: "北京队",
    jiu_jitsu_red_athlete_draw_num: 1,        // 红方签号
    jiu_jitsu_blue_athlete_name: "李四",
    jiu_jitsu_blue_athlete_team: "上海队",
    jiu_jitsu_blue_athlete_draw_num: 2,       // 蓝方签号
    jiu_jitsu_match_round_num: 1,
    jiu_jitsu_match_round_name: "R1",
    jiu_jitsu_match_status: "未开始"
}
```

---

### 4.2 渲染时显示签号

**代码位置**：`public/js/Jiu-Jitsu/jiu-jitsu-bracket-renderer.js` 所有渲染函数

```javascript
// 所有赛制的渲染函数都使用相同的逻辑
const getPid = (name, team, drawNum) => {
    if (!name) return null;
    const key = name + '|' + (team || '');
    if (!participantMap.has(key)) {
        let displayName = name;
        // 如果有签号，显示为 "签号. 姓名" 格式
        if (drawNum != null && drawNum !== '') {
            displayName = `${drawNum}. ${name}`;    // 例如：1. 张三
        }
        participantMap.set(key, { 
            id: pid++, 
            name: team ? `${displayName} (${team})` : displayName,  // 例如：1. 张三 (北京队)
            drawNum: drawNum
        });
    }
    return participantMap.get(key).id;
};

// 调用时传入签号参数
jjMatches.forEach(m => {
    if (m.jiu_jitsu_blue_athlete_name) {
        getPid(
            m.jiu_jitsu_blue_athlete_name, 
            m.jiu_jitsu_blue_athlete_team, 
            m.jiu_jitsu_blue_athlete_draw_num     // 蓝方签号
        );
    }
    if (m.jiu_jitsu_red_athlete_name) {
        getPid(
            m.jiu_jitsu_red_athlete_name, 
            m.jiu_jitsu_red_athlete_team, 
            m.jiu_jitsu_red_athlete_draw_num      // 红方签号
        );
    }
});
```

**说明**：
- 从 `jiu_jitsu_matchs` 表读取签号字段
- 在渲染对阵图时，将签号显示在运动员姓名前面
- 格式：`签号. 姓名 (单位)`，例如：`1. 张三 (北京队)`
- 所有赛制（单败淘汰、双败淘汰、单循环、分区循环）都使用相同的渲染逻辑

---

## 五、完整流程总结

### 5.1 数据流向

```
运动员数据 (athletes 表)
    ↓
生成对阵图 (generateJJBracketForClass)
    ↓
    ├─→ bracket-manager API 创建结构
    │       ↓
    │   bracket_stage, bracket_participant, bracket_round, bracket_match
    │
    └─→ 直接 SQL 补充业务数据
            ↓
        更新 custom_data（签号等信息）
        创建 match_game 记录
        自定义轮次名称
    ↓
同步到业务表 (syncJJMatchesFromBracket)
    ↓
jiu_jitsu_matchs 表（包含签号字段）
    ↓
前端渲染 (JJBracketRenderer)
    ↓
显示对阵图（签号. 姓名）
```

---

### 5.2 混合模式的优势

1. **利用 bracket-manager 的算法**
   - 自动生成对阵图结构
   - 自动计算种子排序
   - 自动处理晋级逻辑

2. **满足业务系统需求**
   - 补充 `event_id`, `category_id` 等业务字段
   - 添加签号、单位等自定义信息
   - 自定义轮次名称、比赛状态等

3. **灵活性和可扩展性**
   - 可以随时添加新的业务字段
   - 可以自定义渲染逻辑
   - 可以支持多种赛制

---

### 5.3 混合模式的挑战

1. **数据一致性**
   - 需要确保 bracket 表和业务表的数据同步
   - 需要处理数据更新和删除的逻辑

2. **维护复杂度**
   - 需要同时理解 bracket-manager API 和数据库表结构
   - 需要处理多种赛制的不同逻辑

3. **性能考虑**
   - 多次 SQL 操作可能影响性能
   - 需要优化批量插入和更新

---

## 六、关键函数列表

### 6.1 生成对阵图

| 函数名 | 文件位置 | 作用 |
|--------|---------|------|
| `generateJJBracketForClass` | jiu-jitsu-bracket-helpers.js:291 | 生成单个级别的对阵图 |
| `generateJJBracketForEvent` | jiu-jitsu-bracket-helpers.js:168 | 生成整个赛事的所有对阵图 |
| `generateJJSeedOrder` | jiu-jitsu-bracket-helpers.js:23 | 生成种子排序（单败淘汰赛） |
| `generateJJRoundRobinSchedule` | jiu-jitsu-bracket-helpers.js:40 | 生成循环赛赛程（Berger 表） |
| `createJJRoundRobinBracketMatches` | jiu-jitsu-bracket-helpers.js:96 | 手动创建循环赛比赛场次 |

### 6.2 同步数据

| 函数名 | 文件位置 | 作用 |
|--------|---------|------|
| `syncJJMatchesFromBracket` | jiu-jitsu-bracket-helpers.js:883 | 从 bracket 表同步到 jiu_jitsu_matchs 表 |

### 6.3 前端渲染

| 函数名 | 文件位置 | 作用 |
|--------|---------|------|
| `renderRoundRobin` | jiu-jitsu-bracket-renderer.js:70 | 渲染单循环赛对阵图 |
| `renderSingleElimination` | jiu-jitsu-bracket-renderer.js:200 | 渲染单败淘汰赛对阵图 |
| `renderDoubleElimination` | jiu-jitsu-bracket-renderer.js:208 | 渲染双败淘汰赛对阵图 |
| `_renderPoolEliminationInline` | jiu-jitsu-bracket-renderer.js:530 | 渲染分区循环赛对阵图 |

---

## 七、数据库表结构

### 7.1 bracket 相关表（bracket-manager 使用）

| 表名 | 作用 | 关键字段 |
|------|------|---------|
| `bracket_stage` | 对阵图阶段 | id, tournament_id, event_id, category_id, type, name |
| `bracket_participant` | 参赛者 | id, tournament_id, name, custom_data |
| `bracket_round` | 轮次 | id, stage_id, group_id, number, name |
| `bracket_match` | 比赛场次 | id, stage_id, round_id, number, opponent1, opponent2, status |
| `bracket_match_game` | 比赛局数 | id, stage_id, parent_id, number |
| `bracket_group` | 分组 | id, stage_id, number, name |

### 7.2 业务表

| 表名 | 作用 | 关键字段 |
|------|------|---------|
| `jiu_jitsu_matchs` | 柔术比赛记录 | event_id, jiu_jitsu_match_categroy, jiu_jitsu_red_athlete_draw_num, jiu_jitsu_blue_athlete_draw_num |
| `athletes` | 运动员信息 | id, athlete_name, athlete_draw_num, athlete_team, athlete_category |

---

## 八、注意事项

1. **签号的重要性**
   - 签号决定比赛的种子位置和顺序
   - 必须在生成对阵图前完成抽签
   - 签号存储在 `athletes.athlete_draw_num` 和 `bracket_participant.custom_data` 中

2. **数据同步时机**
   - 生成对阵图后立即调用 `syncJJMatchesFromBracket`
   - 更新比赛成绩后需要同步回 bracket 表
   - 清除对阵图时需要同时清除两个表的数据

3. **自定义数据格式**
   - `custom_data` 使用 JSON 字符串存储
   - 包含：id, athlete_draw_num, athlete_team, zone
   - 前端渲染时解析 JSON 并显示签号

---

## 九、示例代码

### 9.1 完整的生成流程示例

```javascript
// 1. 准备运动员数据（已抽签）
const athletes = [
    { id: 1, athlete_name: '张三', athlete_draw_num: 1, athlete_team: '北京队' },
    { id: 2, athlete_name: '李四', athlete_draw_num: 2, athlete_team: '上海队' },
    { id: 3, athlete_name: '王五', athlete_draw_num: 3, athlete_team: '广州队' },
    { id: 4, athlete_name: '赵六', athlete_draw_num: 4, athlete_team: '深圳队' }
];

// 2. 使用 bracket-manager 创建对阵图
const stage = await manager.create.stage({
    tournamentId: 1,
    name: '70kg',
    type: 'single_elimination',
    seeding: ['张三', '李四', '王五', '赵六'],
    settings: { manualOrdering: [[1, 4, 3, 2]] }  // 自定义种子排序
});

// 3. 补充业务数据
await db.run(
    'UPDATE bracket_stage SET event_id = ?, category_id = ? WHERE id = ?',
    [1, '70kg', stage.id]
);

// 4. 更新 participant 的 custom_data
for (let i = 0; i < athletes.length; i++) {
    await db.run(
        'UPDATE bracket_participant SET custom_data = ? WHERE name = ?',
        [JSON.stringify({
            id: athletes[i].id,
            athlete_draw_num: athletes[i].athlete_draw_num,
            athlete_team: athletes[i].athlete_team
        }), athletes[i].athlete_name]
    );
}

// 5. 同步到业务表
await syncJJMatchesFromBracket(db, 1, '70kg');

// 6. 前端渲染（显示签号）
// 结果：1. 张三 (北京队) vs 4. 赵六 (深圳队)
```

---

## 十、总结

本系统通过混合使用 bracket-manager API 和直接 SQL 操作，实现了：

1. **自动化对阵图生成**：利用 bracket-manager 的算法能力
2. **业务数据集成**：补充签号、单位等自定义信息
3. **灵活的渲染**：前端可以自由定制显示格式
4. **数据一致性**：通过同步机制确保数据一致

这种混合模式既发挥了 bracket-manager 的优势，又满足了业务系统的特殊需求，是一个高效、灵活、可扩展的解决方案。